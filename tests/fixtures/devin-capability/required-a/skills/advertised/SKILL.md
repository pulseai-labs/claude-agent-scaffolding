---
name: advertised
description: Skill in the default skills/ directory — must appear in inventory
---
This skill lives in the default `skills/` directory and must be discovered when
the plugin is installed.
