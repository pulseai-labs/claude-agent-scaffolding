---
name: excluded
description: Skill in excluded-skills/ — must NOT appear because the skills array omits this directory
---
This skill lives in `excluded-skills/` and must NOT be discovered because the
native manifest's `skills` array does not include this directory.
