# ${PROJECT_NAME} — AI workspace

This is the AI workspace half of a dual-repo project.
Paired canonical repo: `${CANONICAL_ROOT}`.

## What workspace-init declared

- `.workspace/pairing.json` — pairing manifest and repository topology
- `.workspace/init-log` — workspace-init's rollback record
- `CLAUDE.md`, `AGENTS.md`, and `README.md` — temporary topology stubs

## What lives in canonical (`${CANONICAL_ROOT}`)

Production source, tests, product documentation, and product ADRs belong in the canonical once authored.

## Continue with ossify

- Empty canonical: run `/ossify:start`.
- Existing source or history: run `/ossify:adopt`.

`/ossify:start` checks canonical content and routes to `/ossify:adopt` without authoring when it finds either.

## AI-trace filter

A `commit-msg` hook is installed in both this AI workspace and the canonical repo. It blocks:
- `Co-Authored-By:` trailers
- `🤖 Generated with` markers
- `<noreply@anthropic.com>` / `<noreply@openai.com>` references

Override with `git commit --no-verify` only if you understand the implication.
