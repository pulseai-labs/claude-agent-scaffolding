---
scenario_id: 03-legit-flesh
expected_class: flesh
---
A spine "add a CSV export button" works entirely on existing bones (uses the existing report port + UI shell), touches no registered touch surface, and delivers a user journey. No service extraction is proposed; the work stays in the single deployable.
