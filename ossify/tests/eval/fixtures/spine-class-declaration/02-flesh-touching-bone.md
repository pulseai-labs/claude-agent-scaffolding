---
scenario_id: 02-flesh-touching-bone
expected_class: bone
---
A spine declared `flesh` ("add a second order type") plans work whose files include `src/domain/port.rs` — a module listed in the hexagonal-core bone's touch surface. The "add a second order type" spine carries an actor-to-outcome journey — a user submits a second order and sees it recorded. No service extraction is proposed; the work stays in the single deployable.
