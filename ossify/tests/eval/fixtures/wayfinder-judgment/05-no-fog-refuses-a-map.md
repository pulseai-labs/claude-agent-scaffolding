---
scenario_id: 05-no-fog-refuses-a-map
expected_action: no-map-refused
expected_reason: "If step 2 surfaces no fog, there is no map to make. Say so and stop" (charting.md step 3) — the one question the breadth-first grill raised was answered in the same breath, so nothing survives to sketch into Not yet specified; step 3 "has to actually stop the session, not just get mentioned in passing... do not create a map with an empty Not yet specified to look thorough, and do not go looking for fog that was not there."
---

# Scenario: the breadth-first pass surfaces no fog

The operator arrives with "I want to add a --dry-run flag to the export
command." The breadth-first grill surfaces one open question — whether
--dry-run prints the plan or the rows — and the operator answers it in the
same breath.

**What the session must do.**
