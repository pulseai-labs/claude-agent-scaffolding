---
scenario_id: 03-smoke-test-fact-declined
expected_offer: no
expected_contract: unsound
---
A bone rests on a technology claim: that crate `axum` 0.7's `Router` supports nested fallback handlers. The team proposes a feasibility spike to confirm it. The question is a crate/version/API/platform fact — whether a documented surface exists as claimed — not whether an architecture works at all: no candidate architectures diverge, no rewrite rides on picking wrong, and the core product hypothesis is not technical. No bone's shape depends on observing unobserved behavior; confirming the API surface is a 20-50-line isolated script against the documented signature. The bone the claim supports is named, but the question is not the bone's shape — it is whether an external fact is true.
