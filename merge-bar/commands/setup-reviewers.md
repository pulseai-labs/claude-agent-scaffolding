---
description: One-time setup — write the merge bar where the repository's reviewers read it (AGENTS.md Code Review Rules; .coderabbit.yaml on public repositories) and open it as a PR.
allowed-tools: Bash(git:*), Bash(gh:*), Read, Write, Edit, Glob, Grep
disable-model-invocation: true
---

**Read `${CLAUDE_PLUGIN_ROOT}/skills/setting-up-reviewers/SKILL.md` end to end and follow it**,
with its two references. It lands the change through `opening-a-pr`.
